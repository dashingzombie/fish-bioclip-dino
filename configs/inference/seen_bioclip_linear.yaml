defaults: [../base.yaml]
evaluation:
  split: train
  candidate_set: seen
  official_split: test
inference:
  test:
    candidate_set: seen
    mode: bioclip_supervised
model:
  backbone: bioclip2
  tuning_mode: linear_probe
  bioclip_classifier:
    enabled: true
    feature_space: native
