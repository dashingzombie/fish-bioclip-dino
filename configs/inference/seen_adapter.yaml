defaults: [../base.yaml]
evaluation:
  split: train
  candidate_set: seen
  official_split: test
inference:
  test:
    candidate_set: seen
    mode: bioclip_native
model:
  bioclip_image_path:
    enabled: true
    mode: adapter
    adapter:
      enabled: true
      hidden_dim: 512
      dropout: 0.1
